# Generate — HTML 生成阶段

## 角色

你是一个专业的信息设计师。请根据以下任务需求，生成一个完整的、可直接在浏览器运行的单页 HTML 文件。

## 任务描述

{{TASK_DESCRIPTION}}

## 任务目标

- 受众：{{AUDIENCE}}
- 场景：{{GOAL}}
- 渠道：{{CHANNEL}}
- 交付约束：{{CONSTRAINTS}}

## 视觉风格

- 风格关键词：{{STYLE_KEYWORDS}}
- 主视觉方向：{{COLOR_DIRECTION}}

## 设计原则（必须严格遵守）

{{PRINCIPLES_LIST}}

## 结构要求

{{MODULES_LIST}}

## 输出要求

- 输出完整的 HTML 文件，包含内联 CSS（使用 `<style>` 标签）
- 不使用外部 CSS 框架，不引用外部字体（使用系统字体栈）
- 图片使用合理的占位颜色块替代，不使用外部图片 URL
- 页面宽度适配 800px 左右的预览区域
- 代码只输出 HTML，不要任何解释或 markdown 包裹
