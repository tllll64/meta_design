# meta_design

毕业设计原型项目。

## 快速开始

待补充。

## 推送到 GitHub

1. 在 GitHub 上新建同名仓库（不要勾选添加 README/.gitignore）。
2. 将远程地址替换为你创建的仓库地址：

```bash
git remote add origin https://github.com/<your-username>/meta_design.git
git push -u origin main
```
